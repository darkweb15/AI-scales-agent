# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Config
python-dotenv>=1.0.0

# Task queue
celery[redis]>=5.3.0
redis>=5.0.0

# HTTP client
httpx>=0.27.0
requests>=2.31.0

# Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Form data (required for Twilio/voice webhooks)
python-multipart>=0.0.9

# ── Agentic AI stack ──────────────────────────────────────────────────────────
# LangGraph — stateful multi-agent orchestration
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
langchain-openai>=0.2.0
langchain-community>=0.3.0

# RAG — vector store for product knowledge
chromadb>=0.5.0

# Embeddings (free, no API key needed)
sentence-transformers>=3.0.0

# Tokenization for LLM context management
tiktoken>=0.7.0
# ─────────────────────────────────────────────────────────────────────────────

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
hypothesis>=6.100.0
pytest-cov>=5.0.0
